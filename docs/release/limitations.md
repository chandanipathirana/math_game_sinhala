# MVP Limitations

- Uses browser speech synthesis instead of recorded human audio
- Uses local storage only, not cloud sync
- Uses inline visual assets instead of a full illustration pack
- Requires real child testing before production release
- Requires Android device verification before production release
